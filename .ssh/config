# Machine-local hosts and overrides (not tracked).
# ssh_config uses the first obtained value for each option, so the
# local file is included first to let it override the defaults below.
# A missing config.local is silently skipped.
Include config.local

# Shared defaults
ServerAliveInterval 15
ServerAliveCountMax 10
AddKeysToAgent yes
